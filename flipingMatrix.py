# HackerRank preparation


def flippingMatrix(matrix):
    for i in range
    x = matrix[0].index(max(matrix[0]))
    
    result = 0
    for i in range(matrix[0][n],matrix[n][n]):
        result +=n
    return result
